#Использовать 1commands

Команда = Новый Команда;
// Команда.УстановитьПравильныйКодВозврата(0);
Команда.ПоказыватьВыводНемедленно(Истина);
Команда.УстановитьСтрокуЗапуска("chcp 65001 && vrunner decompileepf ./epf ./src/epf");
// Команда.УстановитьСтрокуЗапуска("git add ./src/epf && git diff --quiet && git diff --staged --quiet || git commit -m ""Разбор внешних обработок""");
КодВозврата = Команда.Исполнить();

ЗавершитьРаботу(0);